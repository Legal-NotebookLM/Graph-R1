# Minimal requirements for Graph-R1 Knowledge Graph Building Only
# (No training or inference dependencies)

# Core graph building dependencies
networkx>=3.0
nano-vectordb
numpy>=1.21.0
tiktoken

# Progress bars
tqdm

# Async HTTP (for OpenAI API calls)
aiohttp

# Retry logic
tenacity

# Embeddings and Vector Search (for script_build.py)
FlagEmbedding
faiss-cpu

# LLM for Entity Extraction (default uses OpenAI)
# If you want to use OpenAI API for entity extraction:
openai

# Optional: Only needed if using AWS Bedrock (not needed for OpenAI default)
# aioboto3

# Optional: Only needed if using local HuggingFace models (not needed for OpenAI default)
# transformers
# torch

# Optional: Only needed if using Ollama local models (not needed for OpenAI default)
# ollama

# Optional: Database backends (only if you need them)
# neo4j                    # For Neo4j graph storage
# pymongo                  # For MongoDB KV storage
# oracledb                 # For Oracle storage
# pymilvus                 # For Milvus vector storage
# chromadb                 # For ChromaDB vector storage
# pymysql                  # For TiDB storage
# sqlalchemy               # For SQL databases

# Optional: Additional utilities
# graspologic              # For graph algorithms (node2vec)
# pyvis                    # For graph visualization
